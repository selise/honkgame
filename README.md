## Honkgame (current)

This repository currently ships a **static HTML5 canvas game** with a **SELISE Blocks-backed auth + leaderboard** integration.

- **Start locally (recommended)**:

```bash
docker compose up --build
# open http://localhost:8080
```

For a detailed architecture/config snapshot, see `STATE.md`.

